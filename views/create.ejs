<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset='utf-8'>
    <meta http-equiv='X-UA-Compatible' content='IE=edge'>
    <title><%= post == null ? "Create Post | Scrunch" : "Edit Post | Scrunch" %></title>
    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <link rel='stylesheet' href="/styles/style.css">
    <link rel="stylesheet" href="/theme.css">
    <link rel="stylesheet" href="/index.css">
    <link rel="stylesheet" href="/partials.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #4e73df;
            --primary-hover: #3a5cd0;
            --secondary-color: #f8f9fc;
            --text-color: #333;
            --light-gray: #e3e6f0;
            --border-radius: 8px;
            --box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fc;
            color: var(--text-color);
            display: flex;
            line-height: 1.6;
        }

        .main-content {
            flex: 1;
            padding: 2rem;
            margin-left: 250px; /* Adjust based on your sidebar width */
        }

        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid var(--light-gray);
        }

        h1 {
            font-size: 1.8rem;
            color: #444;
            font-weight: 600;
        }

        .post-form {
            background-color: white;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            padding: 2rem;
            max-width: 800px;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: #555;
        }

        textarea {
            width: 100%;
            min-height: 150px;
            padding: 1rem;
            border: 1px solid var(--light-gray);
            border-radius: var(--border-radius);
            resize: vertical;
            font-family: inherit;
            font-size: 1rem;
            transition: border 0.3s ease;
        }

        textarea:focus {
            outline: none;
            border-color: var(--primary-color);
        }

        .image-input-container {
            position: relative;
        }

        .image-input {
            width: 100%;
            padding: 1rem;
            padding-left: 2.5rem;
            border: 1px solid var(--light-gray);
            border-radius: var(--border-radius);
            font-size: 1rem;
            transition: border 0.3s ease;
        }

        .image-input:focus {
            outline: none;
            border-color: var(--primary-color);
        }

        .image-icon {
            position: absolute;
            left: 0.75rem;
            top: 50%;
            transform: translateY(-50%);
            color: #777;
        }

        .image-preview {
            width: 100%;
            min-height: 200px;
            background-color: #f8f9fc;
            border: 1px dashed var(--light-gray);
            border-radius: var(--border-radius);
            margin-top: 1rem;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .image-preview img {
            max-width: 100%;
            max-height: 300px;
            object-fit: contain;
        }

        .preview-placeholder {
            color: #999;
            text-align: center;
            padding: 2rem;
        }

        .form-actions {
            display: flex;
            justify-content: space-between;
            margin-top: 2rem;
        }

        .btn {
            padding: 0.75rem 1.5rem;
            border-radius: var(--border-radius);
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-primary {
            background-color: var(--primary-color);
            color: white;
            border: none;
        }

        .btn-primary:hover {
            background-color: var(--primary-hover);
            transform: translateY(-2px);
        }

        .btn-secondary {
            background-color: white;
            color: #555;
            border: 1px solid var(--light-gray);
        }

        .btn-secondary:hover {
            background-color: #f1f3f9;
        }

        .character-count {
            margin-top: 0.5rem;
            text-align: right;
            font-size: 0.85rem;
            color: #777;
        }

        .hint {
            font-size: 0.85rem;
            color: #777;
            margin-top: 0.25rem;
        }

        /* Media queries for responsive design */
        @media (max-width: 1024px) {
            .main-content {
                margin-left: 0;
                padding: 1.5rem;
            }
        }

        @media (max-width: 768px) {
            .post-form {
                padding: 1.5rem;
            }
            
            .form-actions {
                flex-direction: column;
                gap: 1rem;
            }
            
            .btn {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <%- include("partials/sidebar.ejs") %>
    
    <div class="main-content">
        <div class="page-header">
            <h1><%= post == null ? "Create New Post" : "Edit Your Post" %></h1>
            <a href="/posts" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Back to Posts
            </a>
        </div>
        
        <div class="post-form">
            <% if (post == null) { %>
                <form action="/create" method="POST" id="postForm">
            <% } else { %>
                <form action="/post/<%= postID %>/edit" method="POST" id="postForm">
            <% } %>
                <div class="form-group">
                    <label for="caption">What's on your mind?</label>
                    <textarea 
                        name="caption" 
                        id="caption" 
                        placeholder="Share your thoughts..." 
                        required
                        maxlength="500"
                    ><%= post ? post.caption : '' %></textarea>
                    <div class="character-count"><span id="charCount">0</span>/500 characters</div>
                </div>
                
                <div class="form-group">
                    <label for="image">Image URL</label>
                    <div class="image-input-container">
                        <i class="fas fa-image image-icon"></i>
                        <input 
                            type="text" 
                            name="image" 
                            id="image" 
                            class="image-input"
                            placeholder="Enter image URL (https://...)" 
                            value="<%= post ? post.imageURL : '' %>" 
                            required
                        >
                    </div>
                    <div class="hint">Enter a direct link to an image (JPG, PNG, GIF formats supported)</div>
                    
                    <div class="image-preview" id="imagePreview">
                        <% if (post && post.imageURL) { %>
                            <img src="<%= post.imageURL %>" alt="Post image">
                        <% } else { %>
                            <div class="preview-placeholder">
                                <i class="fas fa-image fa-3x"></i>
                                <p>Image preview will appear here</p>
                            </div>
                        <% } %>
                    </div>
                </div>
                
                <div class="form-actions">
                    <button type="button" onclick="location.href='/posts'" class="btn btn-secondary">
                        <i class="fas fa-times"></i> Cancel
                    </button>
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-<%= post == null ? 'plus' : 'save' %>"></i>
                        <%= post == null ? "Create Post" : "Save Changes" %>
                    </button>
                </div>
            </form>
        </div>
    </div>

    <script>
        // Image preview functionality
        const imageInput = document.getElementById('image');
        const imagePreview = document.getElementById('imagePreview');
        
        imageInput.addEventListener('input', updateImagePreview);
        
        function updateImagePreview() {
            const imageUrl = imageInput.value.trim();
            
            if (imageUrl) {
                // Check if the preview already contains an image
                let imgElement = imagePreview.querySelector('img');
                
                if (!imgElement) {
                    // If no image exists, create a new one
                    imagePreview.innerHTML = '';
                    imgElement = document.createElement('img');
                    imagePreview.appendChild(imgElement);
                }
                
                // Update image source
                imgElement.src = imageUrl;
                imgElement.alt = 'Post image preview';
                
                // Handle image load errors
                imgElement.onerror = function() {
                    imagePreview.innerHTML = `
                        <div class="preview-placeholder">
                            <i class="fas fa-exclamation-triangle fa-3x" style="color: #e74a3b;"></i>
                            <p>Invalid image URL or image cannot be loaded</p>
                        </div>
                    `;
                };
            } else {
                // If URL is empty, show placeholder
                imagePreview.innerHTML = `
                    <div class="preview-placeholder">
                        <i class="fas fa-image fa-3x"></i>
                        <p>Image preview will appear here</p>
                    </div>
                `;
            }
        }
        
        // Character counter for caption
        const captionTextarea = document.getElementById('caption');
        const charCountElement = document.getElementById('charCount');
        
        captionTextarea.addEventListener('input', updateCharCount);
        
        function updateCharCount() {
            const currentLength = captionTextarea.value.length;
            charCountElement.textContent = currentLength;
            
            // Optional: Add visual indicator when approaching the limit
            if (currentLength > 450) {
                charCountElement.style.color = '#e74a3b';
            } else {
                charCountElement.style.color = '#777';
            }
        }
        
        // Initialize counters and previews on page load
        document.addEventListener('DOMContentLoaded', function() {
            updateCharCount();
            updateImagePreview();
        });
    </script>
</body>
</html>